# Stop Watch

A stop watch with lap times, built with HTML, CSS, JavaScript and Bootstrap.

- Start, pause, resume and reset
- Lap times with best and slowest highlighted
- Progress ring that turns once per minute
- Keyboard shortcuts: `Space` start/pause, `L` lap, `R` reset
- Light and dark theme, responsive

Open `index.html` in a browser. No build step is needed.
